;Вариант 26 
;?? ????????? ?????????????????? 
;?? 29 ???????? ????????? ??? ?????????? ???????
;?? ?????? ????????? ? ?????????????? 
;????? ?????????? ? ????
DATA SEGMENT PARA PUBLIC 'DATA'
    space  db    ' '
    N      dw    29  
    A      db    29 dup (?)
    B      db    29 dup (?)
    vivod db 'C:\out.txt', 0
    
DATA ENDS
;-----------------------------
STK SEGMENT STACK
         DB     256 DUP(0)
STK ENDS
;-----------------------------
CODE SEGMENT PARA PUBLIC 'CODE'
MAIN PROC
    ASSUME CS:CODE, DS:DATA, SS:STK
        MOV         AX,DATA
        MOV         DS,AX
        
;-------INPUT-----------------
        MOV         CX,N
        XOR         DI,DI
        MOV         AH,01h
M5:     INT         21h

        MOV         A[DI],AL
        INC         DI
        CMP         DI,CX
        JL          M5
        
;-----------------------------
      


      
        XOR         DI,DI
        XOR         CX,CX ;schetchk
        XOR         SI,SI 
        MOV         AH,space
        
        MOV         AL,A[DI]
        MOV         B[SI],AL
        INC         DI
        INC         SI
        
M2:     CMP         A[DI],AH ;probel
        JNE         M3
        MOV         AL,A[DI+1]
        MOV         B[SI],Al
        INC         SI
        INC         DI
        INC         CX
        JMP         M1
        
        
M3:     MOV         Al,A[DI]
        MOV         B[SI],AL
        INC         SI
        INC         CX
        
        
        M1:     INC         DIg
        
        CMP         DI,28 ;ne do posledn chisla
        JL          M2
        
        MOV         AL,A[DI]
        MOV         B[SI],AL
        INC         CX
        
                     
        
;-------OUTPUT-----------------
	mov ax,3c00h ;???????? ?????? ????
mov cx,0 ;???????? ?????
mov dx,offset vivod ;???? ? ?????
int 21h
mov bx,ax

mov ax,3d02h ;????????? ???? ??? ??????
mov dx,offset vivod ;???? ? ?????
int 21h

mov ax,4000h ;?????????? ? ???? bx
mov dx,offset B ;????? ?????? ? ????????????? ???????
mov cx,di ;?????????? ???????????? ????????
int 21h

mov ax,3e00h ;????????? ????
int 21h
;-------END-----------------
        MOV AX,4C00h
        INT    21h
MAIN ENDP
CODE ENDS
            END   MAIN